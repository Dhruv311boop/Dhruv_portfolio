{
  "installCommand": "corepack enable && pnpm install --frozen-lockfile --prod=false",
  "buildCommand": "if [ -d artifacts/portfolio ]; then cd artifacts/portfolio; elif [ -d ../portfolio ]; then cd ../portfolio; fi; node node_modules/vite/bin/vite.js build --config vite.config.ts",
  "outputDirectory": "artifacts/portfolio/dist/public",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
