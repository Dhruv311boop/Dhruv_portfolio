{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "pnpm@11.2.2",
  "engines": {
    "node": "22.x",
    "pnpm": "11.2.2"
  },
  "scripts": {
    "build:portfolio": "if [ -d artifacts/portfolio ]; then cd artifacts/portfolio; elif [ -d ../portfolio ]; then cd ../portfolio; fi; node node_modules/vite/bin/vite.js build --config vite.config.ts",
    "build": "corepack pnpm run typecheck && corepack pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "corepack pnpm run typecheck:libs && corepack pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
